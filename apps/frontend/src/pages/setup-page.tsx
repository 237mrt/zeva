export function SetupPage() {
  return (
    <main className="grid min-h-screen place-items-center bg-[var(--zeva-bg)] p-6">
      <div className="rounded-xl border border-[var(--zeva-border)] bg-[var(--zeva-surface)] px-8 py-7 text-center">
        <p className="text-xs font-semibold uppercase tracking-[0.18em] text-[var(--zeva-accent)]">Zeva</p>
        <h1 className="mt-2 text-xl font-semibold">Uygulama altyapısı hazır</h1>
      </div>
    </main>
  );
}
